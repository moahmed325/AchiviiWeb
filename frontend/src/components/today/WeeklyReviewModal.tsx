/**
 * Compatibility re-export for WeeklyReviewModal.
 * The canonical implementation has been relocated to frontend/src/components/review/
 * as part of Milestone M7.2 (Phase 7 Review Flow UI).
 */
export { WeeklyReviewModal, WeeklyReviewModal as default } from '../review';
export type { WeeklyReviewModalProps } from '../review';
